# Documix Roadmap

* [ ] Built-in benchmark like documix benchmark (results saved to benchmark folder, add it to gitignore) for documix conversion methods - which one is the fastest and most accurate. We will fastest method during the test suite so we speedup tests thanks to it.
* [ ] Ranking for extractors (for each type of document, use first availible + switch for method) + method of handling (is install prefered or using what we have)
* [ ] Add --install switch which make sure that documix will install missing dependencies (for example paddleocr)
* [ ] Mention about https://github.com/opendatalab/OmniDocBench in the README (that we trying to implement state of art libs).